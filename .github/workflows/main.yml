name: Build VPK

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build StreamFlix Vita VPK
    runs-on: ubuntu-latest
    container:
      image: vitasdk/vitasdk:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          apk add --no-cache cmake make git bash

      - name: Install vita libraries
        run: |
          vdpm curl
          vdpm jansson
          vdpm openssl
          vdpm zlib
          vdpm libvita2d
          vdpm freetype
          vdpm libpng
          vdpm libjpeg-turbo



      - name: Debug - List files
        run: |
          echo "=== Root ==="
          ls -la
          echo "=== Looking for CMakeLists.txt ==="
          find . -name "CMakeLists.txt" -maxdepth 3

      - name: Configure
        run: |
          # Find where CMakeLists.txt is
          if [ -f "vita/CMakeLists.txt" ]; then
            cd vita
          elif [ -f "CMakeLists.txt" ]; then
            echo "CMakeLists.txt in root"
          else
            echo "ERROR: CMakeLists.txt not found!"
            exit 1
          fi
          mkdir -p build && cd build
          cmake .. -DCMAKE_TOOLCHAIN_FILE=$VITASDK/share/vita.toolchain.cmake

      - name: Build
        run: |
          if [ -d "vita/build" ]; then
            cd vita/build
          elif [ -d "build" ]; then
            cd build
          fi
          make -j$(nproc)

      - name: Upload VPK artifact
        uses: actions/upload-artifact@v4
        with:
          name: streamflix-vita-vpk
          path: |
            **/build/*.vpk
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            **/build/*.vpk
          generate_release_notes: true
